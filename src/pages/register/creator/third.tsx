import { memo } from "react";
import ThirdForm from "../../../components/CreatorForm/ThirdForm";

export const ThirdPage: React.VFC = () => {
  return (
    <>
      <ThirdForm />
    </>
  );
};

export default memo(ThirdPage);
